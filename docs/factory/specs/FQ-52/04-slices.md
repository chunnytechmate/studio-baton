# FQ-52 — Corrected evidence slices

Both slices remain human-owned and under 400 changed lines. Neither enters the generic
ready queue.

1. **Slice 0 / #52 correction tracer** — ship only the approved FQ-52 protocol, preserved
   blocked-run evidence, queue snapshot, and pending-only spec record. Exact inherited CI,
   independent mypy, cold critic, singleton external correction attestation, and human
   merge prove the non-self-referential boundary without claiming bootstrap GREEN.
2. **Slice 1 / #41 green baseline** — after FQ-52 merge, prove permissions, preserve old
   failure links, delete/reclaim the stale branch only with exact owner authorization,
   regenerate/reprove/reapprove the same formatting patch, then require final-head CI,
   critic, attestation, authorized merge-commit provenance, and green `main` closure before
   permitting #40 preclaim.
