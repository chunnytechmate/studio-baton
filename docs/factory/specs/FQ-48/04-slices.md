# FQ-48 — Corrected vertical slices

All slices are Draft, independently judged, under 400 changed lines, and merge-ordered; Gate 4 writes every handoff as waiting.

1. **#41 recovery tracer** — exact approved format patch preserves semantics, all app-bound CI passes, and merged `main` is green.
2. **#40 authority** — a clean clone contains complete concise authority/run schema plus the exact bounded bootstrap allowlist and expiry.
3. **#42 gates** — isolated pinned audit tooling and candidate gates pass deep; format/missing-tool negatives fail closed.
4. **Verifier tooling** — tracked proof/verifier adapters accept exact evidence and reject base, head, worktree, or proof drift.
5. **Implement tooling** — only approved load-bearing handoffs execute; generic bootstrap claims fail and merging expires recovery.
6. **#39 enforcement tracer** — synthetic inventories, previews, explicit apply, and denied-capability probes work without live mutation.
7. **#43 audit primitive** — unique records append with bounded retry; mutation/deletion is `MISCONFIGURED` and opens no audit PR.
8. **#44 readiness flow** — exact-head CI/verifier evidence precedes readiness; resumable cohesive work and old handoffs remain valid.
9. **#45 audit routines** — triage/monitor/tune keep live handoffs but write immutable audit records without product PRs.
10. **#46 activation** — denial probes, strict app-bound checks, reconciliation, and truthful counts precede `enforcement: active`.
