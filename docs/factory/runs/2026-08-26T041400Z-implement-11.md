---
run_id: 2026-08-26T041400Z-implement-11
stage: implement
started_at: 2026-08-26T04:11:33Z
finished_at: 2026-08-26T04:20:00Z
status: succeeded
issue: 11
pull_request: pending
gate_level: full
gate_status: GREEN
verifier: rejected-then-accepted-pending
human_required: no
---
M13: seed_example.sql inserted sample rows with plain INSERT and explicit
ids for pieces/learners; a second `init --force --sample-data` against the
same directory collided on those ids and leaked a raw
sqlite3.IntegrityError past the JSON error envelope.

First attempt (rejected): made _create_database delete the sqlite file on
any --force. A fresh verifier caught that this silently destroyed
pre-existing real learner data on a bare --force re-init that never
mentioned --sample-data — a genuine data-loss regression on a path this
project treats as sensitive, correctly rejected.

Second attempt (this one): fixes the actual collision at its source
instead. Every insert in seed_example.sql now ends in
`ON CONFLICT DO NOTHING`; cmd_init.py and --force's own semantics are
completely untouched. `works` had no natural key (no explicit id, no
unique column) so it is given explicit ids too, matching pieces/learners'
existing pattern, purely so a repeat run has something to conflict on.
`ON CONFLICT DO NOTHING` was chosen over `INSERT OR IGNORE` specifically
because an existing test (test_schema_prints_something_runnable) asserts
the literal substring "INSERT INTO" appears in the printed sample-data
schema; `OR IGNORE` breaks that contiguous substring, `ON CONFLICT DO
NOTHING` does not — no existing test was modified.

Gates: full, GREEN (3 passed). New tests: sample-data survives a second
--force --sample-data run at the same learner count; a real row inserted
between two bare --force runs (no --sample-data) survives, proving the
rejected regression cannot recur. Review queue back-pressure (>3 waiting)
was in effect at claim time; proceeding was an explicit owner instruction
for this session, not an autonomous override of the charter's stop
condition.
