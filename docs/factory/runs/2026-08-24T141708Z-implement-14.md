---
run_id: 2026-08-24T141708Z-implement-14
stage: implement
started_at: 2026-08-24T14:17:08Z
finished_at: 2026-08-24T14:23:55Z
status: succeeded
issue: 14
pull_request: 28
gate_level: full
gate_status: GREEN
verifier: accepted
human_required: false
---

# Implement FQ-14 — add_work raises when PostgREST returns no representation

Interactive session (operator instruction: fix queue items incrementally, with
care). Before claiming, the session completed the stranded handoff of
implement-7: the fix commit and its run record were still local-only while
PR #27 sat empty, so both were pushed (gates re-run GREEN first) before this
run started. Review queue was 2 of 3 — one slot left, so exactly one item was
taken.

## Selection

Issue #14, chosen by the handoff's own confidence (high) and smallest
single-behavior footprint: one conditional return in `PostgrestStore.add_work`.
`src/baton/adapters/db/**` is in the charter's editable zone; not load-bearing.

## Claim

Branch `claude/fq-14` pushed from `origin/main` (`23b2a06`) with empty claim
commit `dc7a371`. First push created the remote ref — claim won. Label moved
`ready-to-implement` → `in-progress`; the write was re-read and corrected once
(the labels endpoint adds rather than replaces, leaving two state labels, so
the stale one was deleted before proceeding).

## Change

- Failing test first: `tests/test_postgrest_add_work.py` (new) — mocks
  `http_request` at the module call site. Empty 2xx body and `null` body each
  must raise `UpstreamError` (both were `DID NOT RAISE` against unfixed code);
  a third test pins the success path returning the created row with its id.
- Fix: one hunk in `PostgrestStore.add_work` — the old silent fallback
  (`return work` when no dict row came back) became an `UpstreamError` whose
  remedy warns the row may exist and a blind retry would duplicate it. The
  dict branch returns exactly as before.
- Diff: 2 files, 122 insertions, 1 deletion. No existing test file touched. No
  LOAD_BEARING path touched. `errors.py`/`exits.py` untouched; `UpstreamError`
  already maps to `Exit.UPSTREAM`.

## Gates

`./.claude/scripts/gates.sh full` → green on first attempt:

`FACTORY_GATES: level=full status=GREEN passed=3 failed=0 failing=none skipped=none misconfigured=none`

## Verification

Delegated to `factory-verifier` with the queue item, branch name, and base SHA
only. Verdict: **accepted**. It read the diff cold, confirmed both mocked
cases exercise real code paths, re-ran gates itself (same GREEN line), and
proved the negative test via `.factory/scripts/prove-test.sh` — reverting only
the src hunk made both error-path tests fail `DID NOT RAISE`
(`PROOF: status=PROVEN test_exit=1`). No reservations.

## Handoff

Commits pushed **before** the PR was opened (the step implement-7 missed).
Draft PR #28 opened with the full template and the verbatim FACTORY_GATES
line. Opened draft per the operator's cautious-increment instruction even
though no draft condition holds. Issue #14 moved to `factory:awaiting-review`
with the PR linked in a comment.

Review queue is now 3 of 3 — at the charter limit. The factory stops taking
new work until a human drains it.
