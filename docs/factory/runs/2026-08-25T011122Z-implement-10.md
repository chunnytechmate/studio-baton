---
run_id: 2026-08-25T011122Z-implement-10
stage: implement
started_at: 2026-08-25T01:11:22Z
finished_at: 2026-08-25T01:38:00Z
status: succeeded
issue: 10
pull_request: 37
gate_level: full
gate_status: GREEN
verifier: accepted
human_required: false
---

# Implement FQ-10 — notes empty-text error and fence/table titles

## Claim

- Review queue check: 1 issue labeled `factory:awaiting-review` (#32) — below the
  charter's stop limit of more than 3.
- Selected FQ-10 (confidence: high; the other four ready items were medium).
- Remote branch claim `claude/fq-10` pushed from `origin/main`
  (base `6b39f71`), first push won. Label moved to `factory:in-progress` and
  re-read to confirm.

## What changed

- `src/baton/cli/cmd_notes.py`
  - `_read_note` distinguishes "argument absent" from "argument empty" using
    `is None`, so `notes push --text ""` now reaches `handle_push`'s existing
    empty-note refusal instead of the misleading "Pass exactly one of --text or
    --file."
  - `_title_for` skips fence lines (``` and ~~~, tracking open/close so fenced
    content is skipped too) and table rows (`| ... |`) when picking a title;
    falls through to the next prose line or the dated fallback.
- `tests/test_notes_text_edge.py` (new) — 4 tests, all red before the fix,
  green after.

## Evidence

- Gates (first run, no iteration needed):
  `FACTORY_GATES: level=full status=GREEN passed=3 failed=0 failing=none skipped=none misconfigured=none`
- Independent verifier (factory-verifier, fresh context): `verdict: accepted`,
  `PROOF: status=PROVEN test_exit=1` via `.factory/scripts/prove-test.sh`.
  No existing test file modified; diff exactly within `files_expected`.

## Notes

- The handoff's done_when says `notes add`; the subcommand is `notes push`
  (no `add` exists). Read as the note-creation path — unambiguous from the
  issue body and code.
- Untracked local files (factory infra, screenshots, `docs/factory/CONTRACT.md`
  etc.) predate this run and were left untouched; they are not part of the diff.
