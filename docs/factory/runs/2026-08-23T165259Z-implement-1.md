---
run_id: 2026-08-23T165259Z-implement-1
stage: implement
started_at: 2026-08-23T16:44:03Z
finished_at: 2026-08-23T16:53:20Z
status: succeeded
issue: 1
pull_request: 3
gate_level: full
gate_status: GREEN
verifier: accepted
human_required: false
---

# Implement FQ-1 — config version gate lets `version: true` through

## Claim

- Review queue at start: 0 issues labeled `factory:awaiting-review` (limit 3) — proceed.
- Branch `claude/fq-1` created from `origin/main` (23b2a06), empty claim commit
  2026-08-23T164403Z-implement-1 pushed without force; push created the remote ref
  (no race lost). Label swapped `ready-to-implement` → `in-progress`, write re-read
  and confirmed before editing code.

## What changed

- `src/baton/core/config.py` — the version gate comparison `version != 1` became
  `isinstance(version, bool) or not isinstance(version, int) or version != 1`.
  Root cause: `True == 1` and `1.0 == 1` in Python, so YAML `version: true` and
  `version: 1.0` passed the gate silently. Float rejection was pre-authorized in
  the issue body and the handoff triage notes.
- `tests/test_config_version_gate.py` — new file, three tests: boolean rejected,
  float rejected, `version: 1` loads identically (profile value, packaged default,
  and version type asserted).

No existing test file modified. No LOAD_BEARING path touched (`src/baton/core/**`
is outside the charter globs; `errors.py`/`exits.py` semantics unchanged).

## Evidence

- Red before fix: both rejection tests failed `DID NOT RAISE ConfigError` on the
  claimed branch before the config.py edit.
- Gates (level=full, first run): `FACTORY_GATES: level=full status=GREEN passed=3
  failed=0 failing=none skipped=none misconfigured=none`.
- Independent verifier (fresh context, given item + branch + base SHA only):
  verdict accepted. It re-ran gates itself (GREEN) and ran
  `./.factory/scripts/prove-test.sh` against base 23b2a06: reverting the config.py
  hunk made the boolean test fail again (`PROOF: status=PROVEN test_exit=1`),
  while the `version: 1` test passed both ways.
- Implementation commit 756d9af; working tree and index clean before verification.

## Handoff

- Draft PR #3 opened with the required template and the verbatim FACTORY_GATES
  line. Draft per explicit operator instruction for this session (no draft
  condition applied on its own).
- Issue 1 moved to `factory:awaiting-review`; PR linked on the issue. A human owns
  the merge decision.

## Not done

Nothing in scope. The optional `version: 1.0` float case was included (same
one-line fix) rather than deferred.
