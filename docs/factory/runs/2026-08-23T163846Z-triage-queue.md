---
run_id: 2026-08-23T163846Z-triage-queue
stage: triage
started_at: 2026-08-23T16:33:00Z
finished_at: 2026-08-23T16:38:46Z
status: succeeded
issue: none
pull_request: 2
gate_level: none
gate_status: not-run
verifier: not-run
human_required: true
---

# Triage run — 2026-08-23 (first run)

First triage of the factory queue; `docs/factory/runs/` was empty before this file,
so every open issue qualified as "untriaged or updated since the last run".

## Scope

- Read `docs/factory/CONTRACT.md` and `docs/factory/CHARTER.md` before acting.
  Charter: TIER `greenfield`, default-deny, tests load-bearing (existing test files
  must not be modified), review-queue stop at >3 items awaiting a human.
- Queried live GitHub issues, not `QUEUE.md`. Open issues: **1** (no pagination
  needed). Cap is 20 — **skipped 0**.
- Verified `factory:ready-to-implement` exists in the repo label set before applying
  it (bootstrap had run).

## Counts per disposition

| Disposition | Count |
|---|---|
| ready-to-implement | 1 |
| ready-to-spec | 0 |
| needs-info | 0 |
| wait-to-implement | 0 |

## Item detail

**FQ-1 — "M6: version: true passes the config version gate because True == 1"**
→ `ready-to-implement` · gate `full` · confidence `high`

- Provenance verified: `review/findings.md:44` (M6, severity กลาง, confirmed by
  running in the 2026-08-16 audit).
- Reproduction (cheap, attempted and confirmed): created a throwaway profile with
  `version: true` and called `baton.core.config.load` — it succeeded, merged
  version was `True`. Root cause is the loose equality at
  `src/baton/core/config.py:328-333` (`True != 1` is False; likewise `1.0 != 1`).
- Charter match: AUTOMATABLE entry "M-level findings.md bug, single-file fix with a
  new failing test". Not LOAD_BEARING (`core/` is in the charter's editable zone).
  2 expected files ≤ 5; expected diff far below the 400-line limit.
- Existing coverage is only `version: 99` rejection (`tests/test_config.py:103`);
  the new test must be a new file, per charter §3.
- Handoff applied on the issue: label `factory:ready-to-implement` (issue had zero
  labels before — nothing removed, no `factory:monitor` to preserve; write re-read
  and confirmed) and a `factory-handoff:v1` comment (issue had no prior comments,
  so no conflicting copy). `triaged_at: 2026-08-23T16:35:57Z`.

## What changed

- GitHub issue #1: label + handoff comment (operational handoff).
- `docs/factory/QUEUE.md`: example entry replaced with the FQ-1 entry. This file
  enters git through PR #2 — `docs/factory/` was previously untracked.
- No code, no tests, no charter/policy files touched. Nothing merged.

## Charter gaps

Things the charter did not cover; each resolved conservatively this run:

1. **The triage routine itself is charter-silent.** Dispositions, the 20-item cap,
   and the queue-entry format come from the skill/contract, not the charter. Under
   a strict default-deny reading, running triage at all needs a human's invocation
   (which this run had). The charter could name triage explicitly.
2. **Optional scope latitude inside an issue body.** Issue #1 invites rejecting
   `version: 1.0` "if it doesn't balloon scope". The charter does not say whether
   issue-body latitude is binding scope or a suggestion. Kept it out of `done_when`
   (recorded in notes) so the done-condition stays checkable either way.
3. **No re-triage cadence or baseline rule.** With no prior run record there was no
   definition of "updated since the last triage". Treated all open issues as in
   scope (1 issue; cap not approached). A staleness bound on triage snapshots is
   undefined.
4. **findings.md line-number drift.** `review/findings.md:253` cites
   `config.py:232-237` but the live code is at `328-333`. Nothing declares which
   source is authoritative for locating a finding; I verified against live code
   and used the issue body's (correct) citation.

## Why the run stopped

Routine completed as designed: one issue in, one disposition out, snapshot + record
written, PR #2 open for a human. Per the skill, no implementation was started for
FQ-1 even though it is ready — discovery and execution stay in separate runs.
