---
run_id: 2026-08-26T011204Z-implement-6
stage: implement
started_at: 2026-08-26T01:12:04Z
finished_at: 2026-08-26T01:47:10Z
status: succeeded
issue: 6
pull_request: 56
gate_level: full
gate_status: GREEN
verifier: accepted
human_required: false
---

# Implement FQ-6 — jobs heartbeat TOCTOU

## Pre-flight

- Read CONTRACT.md then CHARTER.md. Review queue at start: 2 issues labeled
  `factory:awaiting-review` (#10, #32) — below the charter's stop threshold
  (more than 3), so the run could proceed.
- Ready-to-implement candidates: #23, #21, #11, #6 — each carries exactly one
  well-formed `factory-handoff:v1`, all `confidence: medium`. Picked #6, the
  oldest queued item (triaged 2026-08-23T18:20:25Z), touching `core/jobs.py`
  (outside LOAD_BEARING per the charter's editable-zones note).

## Claim

- Branch `claude/fq-6` created from origin/main @ 8409b5e0, empty claim commit,
  first push created the remote ref (no race loss).
- Issue #6 label `factory:ready-to-implement` → `factory:in-progress`, write
  confirmed by re-reading the issue.

## Change

- `done_when`: heartbeat file vanishing between `exists()` and `stat()` must
  not leak a raw `FileNotFoundError` out of `JobRunner.get`.
- Test first: `tests/test_jobs_toctou.py` (new) monkeypatches `Path.exists` to
  still see the heartbeat and `Path.stat` to raise `FileNotFoundError` for it —
  failed on unfixed code with the exact M3 traceback, passed after.
- Fix: `src/baton/core/jobs.py` `get()` wraps the exists→stat pair in
  `suppress(FileNotFoundError)`; a vanished heartbeat reads as
  `heartbeat_age=None`, matching the pre-existing missing-heartbeat branch.
- Diff: 2 files, +78/−1, both inside `files_expected`. No existing test
  touched, no dependency added, no unrelated cleanup.

## Gates

```
FACTORY_GATES: level=full status=GREEN passed=3 failed=0 failing=none skipped=none misconfigured=none
```

First full run was green; no red iterations.

## Verification

- Delegated to `factory-verifier` with queue item, branch, and base SHA only.
- Verdict: **accepted**. It re-ran full gates itself (GREEN) and the reversible
  negative-test proof via `./.factory/scripts/prove-test.sh`
  (`PROOF: status=PROVEN test_exit=1`). Scope, test-tamper, and LOAD_BEARING
  checks all clean.

## Output

- Draft PR #56 opened from `claude/fq-6` with the FACTORY_GATES line quoted
  verbatim; template fully filled.
- Issue #6 moved to `factory:awaiting-review`; PR linked in a comment.
- This run record is the only additional file committed beyond the scoped fix.
