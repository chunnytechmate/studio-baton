---
run_id: 2026-08-24T000806Z-triage-4
stage: triage
started_at: 2026-08-24T00:06:30Z
finished_at: 2026-08-24T00:09:20Z
status: succeeded
issue: 4
pull_request: none
gate_level: none
gate_status: not-run
verifier: not-run
human_required: true
---

# Triage run — incremental: the cap-skipped #4

Local date 2026-08-24 (UTC+7); all UTC stamps below read 2026-08-24T00:0xZ.

## Scope

- Read `docs/factory/CONTRACT.md` and `docs/factory/CHARTER.md` (tier `greenfield`,
  default-deny). Treated both as binding.
- Last triage run record: `2026-08-23T182012Z-triage-queue` (delivered via open PR #25,
  branch `claude/triage-2026-08-24`). This run's intake set:
  - **Issues with no factory state label:** exactly one — **#4 (M1)**, the issue the
    prior run skipped by the 20-item cap and flagged for next-triage-first.
  - **Issues updated since that run record:** none. The newest issue update anywhere in
    the queue is #24 at `2026-08-23T18:20:50Z` — before the prior run finished (18:26Z).
    All other 18:20:xx updates were the prior run's own label/comment writes.
- **Cap: 1 of 1 qualified — 0 skipped by the cap.**
- Not in scope, not counted: #1 (M6) is `factory:awaiting-review` with open PR #3 — the
  contract hands the next decision to a human, so it was not re-triaged. The 19 issues
  labeled by the prior run were not re-triaged (not untriaged, not updated since); their
  live labels were read and are consistent — every open issue now carries exactly one
  state label.
- Stop conditions checked before starting: review queue holds 1 item (#1) — under the
  charter limit of 3. No other condition applied.

## What was checked for #4

- Issue body read in full; treated as untrusted input. Its own scope note confines the
  issue to the first half of finding M1 (duplicate alias under one contact → false
  ambiguity); the second half was already refuted in review round two.
- Issue's line refs date from `fa8a041`. Re-verified at HEAD `23b2a06`:
  - `src/baton/adapters/chat/base.py:92-105` — `exact` is keyed by contact key with
    `setdefault`, and an in-code comment states the exact fix: "the same alias written
    twice under one contact is one match, not two — a duplicate would report an
    ambiguity that does not exist."
  - The fix landed in `cf658ab` ("Make a real send possible…"), already on main.
  - Covering test exists: `tests/test_send.py:392`
    `test_a_duplicate_alias_under_one_contact_is_one_match`, plus
    `test_a_contacts_own_key_wins_over_another_contacts_alias` (:408). Both run this
    session — green.
- Conclusion: **the scoped bug does not reproduce at HEAD because it is already fixed,
  with a passing covering test.** No runtime reproduction needed beyond the two unit
  tests; no services or profiles touched.

## Disposition: #4 → needs-info

| Disposition | Count | Issues |
|---|---|---|
| ready-to-implement | 0 | — |
| ready-to-spec | 0 | — |
| needs-info | 1 | #4 |
| wait-to-implement | 0 | — |

Why not the other three:

- Not `ready-to-implement`: there is nothing left to implement for the scoped bug —
  DONE's red-then-green test cannot exist because the behavior is already green. Also,
  any residual change here touches `adapters/chat/**` (LOAD_BEARING), which the charter
  and the skill both bar from ready-to-implement outright.
- Not `ready-to-spec`: no scope decision exists to make — the described work is done.
  A spec run would be a no-op dressed as progress.
- Not `wait-to-implement`: nothing is blocked on a dependency or unmerged work.

`needs-info` is honest: the only remaining action — confirm fixed-at-HEAD and close, or
name a residual case — is a call only the human owner can make. The charter's default
deny ("everything this charter does not mention") also forbids inventing residual work.

## What changed

- GitHub #4: applied label `factory:needs-info` (no prior state label to remove; no
  `factory:monitor` present to preserve).
- GitHub #4: created `<!-- factory-handoff:v1 -->` comment
  (issuecomment-5389253764) carrying disposition, the specific close-vs-residual
  question, done_when, files_expected, load_bearing: true, gate_level: deep, confidence:
  high, triaged_at `2026-08-24T00:08:06Z`.
- Files: `docs/factory/QUEUE.md` — FQ-4 entry added, header updated, "Not covered"
  reduced to FQ-1; this run record. Branch `claude/triage-2026-08-24-fq4`, **stacked on
  the prior triage branch `claude/triage-2026-08-24` (PR #25)** so the snapshot
  accumulates instead of forking the audit trail. Rebasing this content onto main
  instead would have made this PR *replace* PR #25's 20-issue snapshot — worse for
  audit. No source, test, docs-claim, or factory-policy files were touched.
- Nothing was implemented; no issue was claimed; no merge.

## Charter gaps (for a human)

New this run:

1. **No disposition for "already fixed at HEAD."** The four dispositions all assume live
   work remains. An issue whose bug is demonstrably fixed with a passing covering test
   has no route in the charter or contract — used needs-info with the close-vs-residual
   question as the closest fit. A fifth outcome (verified-fixed → human closes) would
   remove the guesswork.
2. **No routine is authorized to close an issue.** The contract grants labels and
   handoff comments, not closures. This run assumed closing stays human-only and did not
   close #4 even though the evidence is green. If agents may close as already-fixed, the
   contract should say so.

Still open from run `2026-08-23T182012Z` (unanswered, restated so they are not lost):
single-file-fix vs new-test-file counting; route for findings whose fix is an
existing-test edit; whether in-source comments count as "docs" for claim fixes; and the
re-verification duty for aged findings.

Also noted, not a charter gap: the instructed PR title "factory: triage 2026-08-24"
collides with already-open PR #25. This run opened a second PR with that title (stacked,
cross-referenced) rather than altering the instructed title.
