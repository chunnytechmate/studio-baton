---
run_id: 2026-08-23T182012Z-triage-queue
stage: triage
started_at: 2026-08-23T18:05:40Z
finished_at: 2026-08-23T18:26:00Z
status: succeeded
issue: none
pull_request: none
gate_level: none
gate_status: not-run
verifier: not-run
human_required: false
---

# Triage run — first recorded triage of the GitHub queue

Local date 2026-08-24 (UTC+7); all UTC stamps below read 2026-08-23T18:xxZ.

## Scope

- Read `docs/factory/CONTRACT.md` and `docs/factory/CHARTER.md` (tier `greenfield`,
  default-deny). Treated both as binding.
- No prior triage run record exists under `docs/factory/runs/`, so every untriaged open
  issue qualified. 21 open issues carried no factory state label (#4–#24); #1 (M6) is
  `factory:awaiting-review` with an open PR and was not re-triaged.
- **Cap: 20 of 21 — 1 skipped.** Skipped: **#4 (M1, duplicate contact alias ambiguity)**,
  the least recently updated of the qualifying set. It remains untriaged; the next triage
  run must take it first. Recorded in `docs/factory/QUEUE.md` under "Not covered this run".
- All 20 issues were bulk-filed from `review/findings.md` M-items on 2026-08-23 (per its
  changelog). Issue bodies were treated as untrusted input; every claimed code location
  was re-verified against HEAD before classification.

## What was checked

- Every issue body was read in full. Proposed `done_when` texts in bodies were treated as
  suggestions, not decisions.
- Code spots re-verified at HEAD `23b2a06` (the findings' line refs date from `fa8a041`):
  `core/jobs.py` (prune count :481-486, get TOCTOU :337, supervise meta-first :273,
  handler-after-Popen :294-310, spawn pre-creates meta :200-215), `adapters/db/postgrest.py`
  (add_work :306-310), `cli/cmd_notes.py` (:81-84, :95-111), `cli/cmd_init.py` (:226-232,
  :252), `cli/cmd_calendar.py` (:322 guard present), `cli/cmd_send.py` (:163),
  `adapters/media/ffmpeg.py` (:24-29), `core/config.py` (:55-75), grep for `degraded`
  readers, `src/baton/migrations/` location vs `contracts/**`.
- No finding had drifted away at HEAD. One shrank: M15's harmful half is already guarded
  (`len(event.start) >= 16`), leaving a display decision — classified accordingly.
- No runtime reproduction was attempted this run: every item already carries recorded
  confirmation evidence in `review/findings.md` (run audit 2026-08-16 or code read), and
  re-running would need services or profiles. Repro fields cite that evidence plus the
  HEAD re-verification.
- Stop conditions checked: review queue holds 1 item (#1, awaiting-review) — under the
  charter's limit of 3, so triage proceeded. No other condition applied.

## Dispositions (20)

| Disposition | Count | Issues |
|---|---|---|
| ready-to-implement | 7 | #6, #7, #10, #11, #14, #21, #23 |
| ready-to-spec | 13 | #5, #8, #9, #12, #13, #15, #16, #17, #18, #19, #20, #22, #24 |
| needs-info | 0 | — |
| wait-to-implement | 0 | — |

Ready-to-implement rationale: each is an M-level finding matching charter AUTOMATABLE
(single-file source fix + new failing test, or error-envelope conformance in `cli/`),
touches no LOAD_BEARING glob, no existing test file, ≤5 files, with a checkable
done_when. #21's clarity depends on `spawn()` pre-creating job meta before launching
`supervise`, so requiring an existing job cannot break the normal path.

Ready-to-spec rationale (the binding reasons): owner decisions named in the findings
(#5, #8, #9, #12, #15, #18, #20, #22, #24); exit-code semantics for existing callers
(#12, #22); LOAD_BEARING `adapters/chat/**` (#16, deep gates, currently fail-closed until
pip-audit is installed); edits to existing test files forbidden unattended by charter §3
(#17 `tests/fakes.py`, #19 `tests/test_resolve.py`); display/product choice with the
hazard already guarded (#13).

needs-info is empty honestly, not by shortcut: all 20 have clear intent and recorded
confirmation; the open questions are decisions, which route through factory-spec.

## What changed

- GitHub: one `factory:*` state label applied to each of the 20 issues (none had a prior
  state label; nothing to remove; `factory:monitor` not present on any).
- GitHub: one `<!-- factory-handoff:v1 -->` comment created per issue (none existed),
  carrying disposition, done_when, files_expected, load_bearing, gate_level, confidence,
  triaged_at `2026-08-23T18:20:00Z` (plus `question:` on spec items).
- Files: `docs/factory/QUEUE.md` created (snapshot, 20 entries + not-covered section);
  this run record. No source, test, docs-claim, or factory-policy files were touched.
- Nothing was implemented; no issue was claimed; no merge.

## Charter gaps (for a human)

1. **"Single-file fix" vs the required new test.** AUTOMATABLE entry 1 says M-level
   single-file fixes with a new failing test, but does not say whether the new test file
   counts toward "single-file". This run assumed new test files do not count (DONE
   requires one). Recorded as an assumption, not a rule.
2. **No route for findings whose fix is an existing-test edit.** M22 (#17) and M24 (#19)
   are real work whose core change is editing an existing test file, which charter §3
   forbids unattended. The charter offers no disposition for this shape; this run used
   ready-to-spec with an explicit owner-approval question. A named rule would remove the
   doubt.
3. **Claim fixes inside source files.** AUTOMATABLE entry 3 covers claims in README/docs;
   M19's (#15) false claim lives in an in-source comment/preset name. Whether source
   comments count as "docs" is unsaid. Routed to ready-to-spec anyway (the issue also
   offers a behaviour-changing option), but the boundary is undefined.
4. **Re-verification duty when findings age.** Findings reference commit `fa8a041` while
   HEAD is `23b2a06`; the charter does not say whether triage must re-verify at HEAD.
   This run re-verified all 20 (all still present; M15 shrank). Without a rule, a future
   triage may classify from stale line numbers.
