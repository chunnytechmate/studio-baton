---
run_id: 2026-08-24T011200Z-implement-7
stage: implement
started_at: 2026-08-24T01:12:00Z
finished_at: 2026-08-24T01:20:37Z
status: succeeded
issue: 7
pull_request: 27
gate_level: full
gate_status: GREEN
verifier: accepted
human_required: false
---

# Implement FQ-7 — prune counts only dirs it actually removed

Unattended scheduled run. Review queue was at 1 of the 3 allowed `factory:awaiting-review`
items, so the run proceeded.

## Selection

Seven open `factory:ready-to-implement` issues, all with well-formed `factory-handoff:v1`
comments. Confidence: high for #7, #10, #14; medium for #6, #11, #21, #23. Among the
high-confidence items #7 was the smallest single-behavior fix (one function's return value),
in `src/baton/core/jobs.py` — an editable zone per the charter. Not load-bearing.

## Claim

Branch `claude/fq-7` pushed from `main` (`23b2a06`) with empty claim commit
`e7ef103` ("factory: claim FQ-7 (2026-08-24T011200Z-implement-7)"). First push created the
remote ref — claim won without a race. Label moved `ready-to-implement` → `in-progress`
and re-read to confirm.

## Change

- Failing test first: `tests/test_jobs_prune_count.py` (new) — ages two finished jobs past
  the cutoff, blocks `rmdir` on one with an `artifacts/` subdirectory, asserts
  `removed == 1` with the clean dir gone and the blocked dir still on disk. RED confirmed
  against unfixed code (`assert 2 == 1`).
- Fix: one hunk in `JobRunner.prune` — increment `removed` only when `entry.rmdir()`
  succeeds; on `OSError`, `continue`. The unconditional `removed += 1` behind
  `suppress(OSError)` was counting dirs that survived on disk.
- Diff: 2 files, 76 insertions, 1 deletion. No existing test file touched. No
  LOAD_BEARING path touched.

## Gates

`./.claude/scripts/gates.sh full` → green on first attempt:

`FACTORY_GATES: level=full status=GREEN passed=3 failed=0 failing=none skipped=none misconfigured=none`

## Verification

Delegated to `factory-verifier` with the queue item, branch name, and base SHA only.
Verdict: **accepted**. It re-ran gates itself (same GREEN line) and proved the negative
test via `.factory/scripts/prove-test.sh` (`PROOF: status=PROVEN test_exit=1` — with the
fix reverted the new test fails `assert 2 == 1`). No rejections; one pass.

## Handoff

Draft PR #27 opened with the full template and the verbatim FACTORY_GATES line. Issue #7
moved to `factory:awaiting-review` with the PR linked in a comment. Review queue now at
2 of 3.
