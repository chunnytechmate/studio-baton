"""The work Baton actually does, expressed against adapter Protocols only.

Nothing here imports a vendor SDK or reads configuration directly, which is
what lets every pipeline be tested offline against `baton.adapters.fakes`.
"""
