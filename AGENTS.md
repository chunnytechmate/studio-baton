# Project and factory guidance for Codex

Read `CLAUDE.md` for this project's commands and conventions, then read
`docs/factory/CONTRACT.md` and `docs/factory/CHARTER.md` before changing anything.
The contract is shared with Claude Code; if this adapter and the contract disagree, the
contract wins. Factory workflow skills and hooks are adopted by later bootstrap slices
(#44–#45); until then the tracked contract is the whole operating model, and GitHub
branch protection remains the enforcement boundary.
